#include <stdio.h>
#include <unistd.h>
/**
 * main - main function
 * Description: print positive and negative value by using if statement
 * Return: 0
 */
int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');

return (0);
}
